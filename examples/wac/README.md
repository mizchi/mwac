# WAC compose example (fixtures)

This directory mirrors the `bytecodealliance/wac` compose example and is used
as a reference fixture for `moon-component` plug/compose behavior.

Files:
- `script.wac` (compose script)
- `deps/example/hello.wasm`
- `deps/example/greeter.wasm`
